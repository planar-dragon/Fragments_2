package com.skillbox.BaseMethods

data class Animal ( // Создаем дата класс Animal с полями(свойствами) energy (энергия), weight (вес), name (имя)
    val energy: Int, // energy (энергия)
    val weight: Int, // weight (вес)
    val name: String // name (имя)
)