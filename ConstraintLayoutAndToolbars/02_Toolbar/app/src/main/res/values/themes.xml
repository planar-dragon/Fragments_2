<resources>
    <!-- Base application theme. -->
    <style name="Theme.Toolbar_15_8" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status Bar color. -->
        <item name="android:statusBarColor">#FF383838</item>
        <!-- Status Bar text color. -->
        <item name="android:textColorLink">#FFFFE14A</item>
    </style>

    <!--     Style Bar. -->
    <style name="ToolbarStyle">
        <item name="android:background">@color/white</item>
        // текст тулбара из ночной темы
        <item name="android:theme">@style/ThemeOverlay.AppCompat.Dark.ActionBar</item>
        <item name="android:elevation">20dp</item>
        <item name="android:textSize">5sp</item>
        // стиль применяемый к всплывающему меню
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Light</item>
    </style>
</resources>

